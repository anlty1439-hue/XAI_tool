:root{
  --bg:#0f172a;
  --card:#0b1226;
  --muted:#94a3b8;
  --text:#e5e7eb;
  --primary:#22c55e;
  --secondary:#38bdf8;
  --danger:#f87171;
  --accent:#a78bfa;
  --border:#1f2937;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Ubuntu,Arial,sans-serif;
  color:var(--text);
  background:linear-gradient(180deg,#0b1022, #091126 35%, #0a132b 100%);
}

.app-header, .app-footer{
  backdrop-filter: blur(8px);
  background: rgba(10,16,32,0.6);
  border-bottom:1px solid var(--border);
}
.app-header{
  position:sticky; top:0; z-index:10;
  display:flex; align-items:center; justify-content:space-between;
  padding:12px 20px;
}
.brand{display:flex; align-items:center; gap:12px}
.brand-mark{
  width:36px; height:36px; border-radius:10px;
  display:grid; place-items:center;
  background:linear-gradient(135deg, var(--secondary), var(--accent));
  color:#0b1020; font-weight:700;
}
.brand-title{display:block; font-weight:700}
.brand-subtitle{display:block; font-size:12px; color:var(--muted)}

.top-nav .link{
  background:none; color:var(--text); border:1px solid transparent;
  padding:8px 10px; border-radius:8px; cursor:pointer;
}
.top-nav .link:hover{border-color:var(--border); background:#0a132b}

main#app{max-width:1100px; margin:0 auto; padding:24px 16px}
.h1{font-size:clamp(28px,4vw,40px); margin:8px 0}
.lead{color:var(--muted); max-width:64ch}

.hero{
  display:grid; gap:16px; justify-items:start; text-align:left;
  padding:32px; border:1px solid var(--border); border-radius:16px;
  background:
    radial-gradient(600px 200px at 90% -10%, rgba(56,189,248,0.15), transparent 60%),
    radial-gradient(600px 200px at 10% 120%, rgba(34,197,94,0.12), transparent 60%),
    linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0));
}
.hero-illustration{font-size:56px}

.panel{
  background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0));
  border:1px solid var(--border);
  border-radius:16px; padding:20px;
}

.actions{display:flex; gap:12px; margin-top:16px}
.actions.wrap{flex-wrap:wrap}

.btn{
  background:#111827; color:var(--text);
  border:1px solid var(--border); border-radius:10px;
  padding:10px 14px; cursor:pointer;
}
.btn:hover{background:#0f172a}
.btn-primary{
  background:linear-gradient(135deg, var(--primary), #16a34a);
  border-color:#16a34a; color:#071a0d; font-weight:700;
}
.btn-secondary{
  background:linear-gradient(135deg, var(--secondary), #0ea5e9);
  border-color:#0ea5e9; color:#04131a; font-weight:700;
}

.link{
  background:none; color:var(--secondary); border:none; cursor:pointer;
}
.link:hover{color:#7dd3fc}

.table-scroll{overflow:auto; border:1px solid var(--border); border-radius:12px}
.table{
  width:100%; border-collapse:collapse; min-width:680px;
}
.table th, .table td{
  border-bottom:1px solid var(--border);
  padding:10px; text-align:left;
}
.table thead th{color:#cbd5e1; font-weight:600; background:#0a132b}
.table tbody tr:hover{background:#0b142e}

.muted{color:var(--muted)}

.route{display:none}
.route.active{display:block}

.toast{
  position:fixed; left:50%; transform:translateX(-50%);
  bottom:24px; padding:12px 16px; border-radius:10px;
  background:#0b142e; border:1px solid var(--border);
  color:var(--text); min-width:240px; text-align:center;
  opacity:0; pointer-events:none; transition:opacity .2s, bottom .2s;
  z-index:20;
}
.toast.show{opacity:1; bottom:32px}

.modal{
  position:fixed; inset:0; display:grid; place-items:center;
  background:rgba(2,6,23,0.6); opacity:0; pointer-events:none; transition:.2s;
  z-index:30; padding:16px;
}
.modal.show{opacity:1; pointer-events:auto}
.modal-content{
  background:#0a132b; color:var(--text); border:1px solid var(--border);
  border-radius:14px; padding:20px; max-width:540px; width:100%;
}

.drawer{
  position:fixed; right:0; top:0; height:100%; width:min(520px, 100%);
  background:#0a132b; border-left:1px solid var(--border);
  transform:translateX(100%); transition:transform .25s; z-index:25;
}
.drawer.open{transform:translateX(0)}
.drawer-content{height:100%; display:flex; flex-direction:column}
.drawer-header{
  display:flex; justify-content:space-between; align-items:center;
  padding:16px; border-bottom:1px solid var(--border);
}
.icon-btn{
  background:#0f172a; color:var(--text); border:1px solid var(--border);
  padding:6px 10px; border-radius:8px; cursor:pointer;
}
.drawer-body{padding:16px; overflow:auto; color:#d1d5db}

.graph-area{
  display:grid; place-items:center; padding:16px; margin:16px 0;
  border:1px dashed #334155; border-radius:12px; min-height:280px;
  background:repeating-linear-gradient(
    45deg, rgba(148,163,184,0.06), rgba(148,163,184,0.06) 10px,
    rgba(148,163,184,0.03) 10px, rgba(148,163,184,0.03) 20px
  );
}
.graph-area img{
  max-width:100%; height:auto; border-radius:10px; border:1px solid var(--border);
  background:#091126;
}

.viz-grid{
  display:grid; gap:16px;
  grid-template-columns: repeat(auto-fill, minmax(220px,1fr));
}
.viz-card{
  border:1px solid var(--border); border-radius:12px; padding:12px;
  background:#0b142e; display:flex; flex-direction:column; gap:10px;
}
.viz-thumb{
  display:grid; place-items:center; background:#0a132b; border:1px solid var(--border);
  border-radius:8px; padding:8px; height:140px;
}
.viz-title{font-weight:600}

.features-list{display:grid; gap:8px}
.features-list .row{
  display:flex; justify-content:space-between; gap:12px;
  padding:8px 10px; border:1px solid var(--border); border-radius:8px; background:#0b142e;
}
.features-list .row .k{color:#cbd5e1}
.features-list .row .v{color:#9adea2}

.app-footer{
  margin-top:40px; padding:16px 20px; border-top:1px solid var(--border);
}
.foot-inner{display:flex; gap:8px; align-items:center; justify-content:center; color:var(--muted); flex-wrap:wrap}
.sep{opacity:.5}
